apiVersion: v1
kind: Service
metadata:
  name: loadbalancer-httpd-service
spec:
  selector:
    name: deployment
  type: LoadBalancer  
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30000